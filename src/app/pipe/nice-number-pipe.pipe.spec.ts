import { NiceNumberPipePipe } from './nice-number-pipe.pipe';

describe('NiceNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NiceNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
